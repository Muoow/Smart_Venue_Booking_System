spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/courtflow?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    password: admin123
  data:
    redis:
      repositories:
        enabled: false
management:
  endpoints:
    web:
      exposure:
        include: prometheus
courtflow:
  demo:
    enabled: true
  middleware:
    enabled: false
