spring:
  datasource:
    url: jdbc:mysql://mysql:3306/courtflow
    username: admin
    password: admin123
  data:
    redis:
      host: redis
      port: 6379
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: admin
    password: admin123
    connection-timeout: 1s
    template:
      retry:
        multiplier: 2
        enabled: true
    listener:
      direct:
        acknowledge-mode: auto
management:
  endpoints:
    web:
      exposure:
        include: prometheus